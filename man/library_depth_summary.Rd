% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_qc.R
\name{library_depth_summary}
\alias{library_depth_summary}
\title{Per-sample library depth summary.}
\usage{
library_depth_summary(counts, meta = NULL, group_col = NULL)
}
\arguments{
\item{counts}{Numeric matrix or data.frame (rows = features,
cols = samples).}

\item{meta}{Optional data.frame with at least a `samples` column matching
`colnames(counts)` plus the column named in `group_col`. If NULL,
`group` is `NA_character_` for every sample.}

\item{group_col}{Name of the column in `meta` to use as the group label.
Ignored if `meta` is NULL.}
}
\value{
data.frame with columns `sample`, `depth`, `group`,
  `is_outlier_2sd` (one row per sample, ordered as `colnames(counts)`).
}
\description{
Computes column sums (sequencing depth) for a count matrix, optionally
annotates each sample with a group label from a metadata data.frame, and
flags outliers using a 2-sigma rule on the depth values.
}
\examples{
m <- matrix(rpois(60, lambda = 10), nrow = 10,
            dimnames = list(NULL, paste0("s", 1:6)))
library_depth_summary(m)
}
