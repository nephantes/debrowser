% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_gsea.R
\name{nes_heatmap_data}
\alias{nes_heatmap_data}
\title{Reshape per-comparison GSEA results for the NES heatmap.}
\usage{
nes_heatmap_data(results_by_comparison, sig_only = FALSE, sig_threshold = 0.05)
}
\arguments{
\item{results_by_comparison}{Named list of \code{\link{run_gsea}}
outputs. Names become column labels in the heatmap.}

\item{sig_only}{Logical; if TRUE, retain only pathways significant in
at least one comparison.}

\item{sig_threshold}{padj cutoff used when `sig_only = TRUE`.}
}
\value{
data.frame with columns `pathway`, `comparison`, `NES`, `padj`.
}
\description{
Used by \code{\link{enrichmentNesHeatmapServer}} to render a tile plot
of NES across pathways (rows) by comparisons (columns).
}
\examples{
\dontrun{
nes_heatmap_data(list(c1 = run_gsea(de1, paths),
                      c2 = run_gsea(de2, paths)))
}
}
