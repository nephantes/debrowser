% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_method_concordance.R
\name{de_direction_summary}
\alias{de_direction_summary}
\title{Per-entry up / down / total significant gene counts.}
\usage{
de_direction_summary(de_list, padj_cutoff = 0.05, lfc_cutoff = 0)
}
\arguments{
\item{de_list}{Named list of DE result data.frames; each must
contain `padj` and `log2FoldChange` columns.}

\item{padj_cutoff}{Maximum padj for "significant" (default 0.05).}

\item{lfc_cutoff}{Minimum |log2FoldChange| for "significant"
(default 0).}
}
\value{
data.frame with columns `comparison`, `n_up`, `n_down`,
  `n_sig` (= `n_up + n_down`). One row per entry of `de_list`,
  in input order.
}
\description{
Counts significant genes per entry of a named DE-results list at the
given padj / |log2FoldChange| cutoffs. Up = significant + log2FC > 0;
Down = significant + log2FC < 0. NA padj / NA log2FoldChange are
treated as not-significant.
}
\examples{
de_list <- list(
  comp1 = data.frame(
    log2FoldChange = c(2, -1, 0, 3, -2),
    padj = c(0.01, 0.04, 0.5, 0.02, 0.03)
  ),
  comp2 = data.frame(
    log2FoldChange = c(1.5, -0.8, 0.2, 2.1, -1.5),
    padj = c(0.02, 0.03, 0.6, 0.01, 0.04)
  )
)
de_direction_summary(de_list, padj_cutoff = 0.05)
}
