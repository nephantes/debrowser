% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_normalize.R
\name{apply_batch_correction}
\alias{apply_batch_correction}
\title{Apply batch-effect correction.}
\usage{
apply_batch_correction(
  counts,
  metadata,
  method = "none",
  batch_col = NULL,
  treatment_col = NULL
)
}
\arguments{
\item{counts}{Numeric matrix (genes x samples).}

\item{metadata}{data.frame; first column is sample id.}

\item{method}{One of "none", "Combat", "CombatSeq", "Harman".}

\item{batch_col}{Name of the batch column in `metadata`.}

\item{treatment_col}{Name of the treatment column in `metadata`. May be
NULL or "None" — only required for Harman.}
}
\value{
Corrected count matrix.
}
\description{
Pure function — no Shiny dependency. Accepts batch / treatment column
names directly instead of pulling from a reactive `input` object.
}
\examples{
m <- matrix(as.integer(c(100, 200, 150, 80, 250, 130,
                         40,  60,  55, 30,  70,  45)),
            nrow = 2, byrow = TRUE)
colnames(m) <- paste0("S", 1:6)
rownames(m) <- c("Gene1", "Gene2")
# method = "none" returns the matrix unchanged
apply_batch_correction(m, metadata = NULL, method = "none")
}
