% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_prep_data.R
\name{apply_merged_filters}
\alias{apply_merged_filters}
\title{Apply Up/Down cutoffs across a merged-comparisons table.}
\usage{
apply_merged_filters(dc, nc, params = list())
}
\arguments{
\item{dc}{List of per-comparison containers (each has `init_data`, `cols`,
`cond_names`).}

\item{nc}{Number of comparisons.}

\item{params}{Named list with `norm_method`.}
}
\value{
Merged data.frame with a `Legend` column (`"Sig"` / `"NS"`).
}
\description{
Pure version of `applyFiltersToMergedComparison()`.
}
\examples{
init1 <- data.frame(
  S1 = c(100L, 40L), S2 = c(200L, 60L),
  S3 = c(10L, 150L), S4 = c(12L, 160L),
  foldChange = c(10, 0.25), padj = c(0.01, 0.01),
  row.names = c("GeneA", "GeneB")
)
dc <- list(list(
  init_data = init1,
  cols = c("S1", "S2", "S3", "S4"),
  cond_names = c("Treat", "Ctrl")
))
params <- list(norm_method = "none", padj_cutoff = 0.05, fold_cutoff = 2)
apply_merged_filters(dc, nc = 1, params = params)
}
