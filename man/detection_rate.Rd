% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_qc.R
\name{detection_rate}
\alias{detection_rate}
\title{Per-sample gene detection rate.}
\usage{
detection_rate(counts, threshold = 0)
}
\arguments{
\item{counts}{Numeric matrix or data.frame (rows = features,
cols = samples).}

\item{threshold}{Detection threshold (default 0). A feature is "detected"
in a sample iff its value is strictly greater than `threshold`.}
}
\value{
data.frame with columns `sample`, `n_detected`, `total_features`,
  `detection_pct` (one row per sample).
}
\description{
Counts the number of features with values strictly greater than
`threshold` per sample and reports detection percentage.
}
\examples{
m <- matrix(c(0, 0, 5, 5, 0, 5), nrow = 3,
            dimnames = list(NULL, c("a", "b")))
detection_rate(m)
}
