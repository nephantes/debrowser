% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/de_progress.R
\name{update_progress}
\alias{update_progress}
\title{Push a progress update to the browser for one key.}
\usage{
update_progress(session, key, state)
}
\arguments{
\item{session}{Shiny session object.}

\item{key}{character, progress key.}

\item{state}{character, progress state.}
}
\value{
Invisibly NULL.
}
\description{
Sends a Shiny custom message of type "debrowser-progress" with a
payload built by `progress_message(key, state)`. The handler installed
by `getTabUpdateJS()` (R/funcs.R) reads the message and toggles CSS
classes on the matching icon span and parent pill.
}
\examples{
\donttest{
  if (interactive()) {
    update_progress(session, "upload", "done")
  }
}

}
