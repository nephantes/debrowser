% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uifuncs.R
\name{de_nav_chip}
\alias{de_nav_chip}
\title{de_nav_chip}
\usage{
de_nav_chip(num, label, progress_pill = NULL)
}
\arguments{
\item{num}{integer/character — the chip number (1..N)}

\item{label}{character — the tab label}

\item{progress_pill}{optional progress pill key (e.g. `"data_prep"`)}
}
\value{
an `htmltools::HTML` blob suitable for `nav_panel(title = ...)`
}
\description{
B3.2 helper. Renders an HTML title for `bslib::nav_panel(title = ...)`
that consists of a small numbered chip + the panel name. Matches the
mockup header pattern (numbered tabs like in aidrift.geniohub.com).
Optionally accepts a `de_progress_label()`-style progress-pill marker
so the Data Prep tab keeps its workflow checkmark behavior.
}
\examples{
x <- de_nav_chip(1, "Data Prep")
}
