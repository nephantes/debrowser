% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_qc.R
\name{qc_keep_cols}
\alias{qc_keep_cols}
\title{Subset a count matrix to a user-selected column list.}
\usage{
qc_keep_cols(counts, selected = NULL)
}
\arguments{
\item{counts}{Numeric matrix or data.frame, or NULL.}

\item{selected}{Character vector of sample names to keep, or NULL.}
}
\value{
The subset matrix (or `counts` unchanged if `selected` is
  NULL); NULL passes through.
}
\description{
Mirrors the QC sidebar's column-selector contract: NULL `selected`
(the pre-render state) returns `counts` unchanged; otherwise keeps
only the columns whose name is in `selected`. Empty intersection
returns a zero-column matrix (matches \code{getSelectedCols} on an
empty selection).
}
\examples{
m <- matrix(1:6, nrow = 2,
            dimnames = list(NULL, c("a", "b", "c")))
qc_keep_cols(m, c("a", "c"))
qc_keep_cols(m, NULL)  # unchanged
}
