% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_qc.R
\name{qc_keep_meta_rows}
\alias{qc_keep_meta_rows}
\title{Subset a sample-metadata data.frame by a user-selected column list.}
\usage{
qc_keep_meta_rows(meta, selected = NULL)
}
\arguments{
\item{meta}{data.frame with a `samples` column, or NULL.}

\item{selected}{Character vector of sample names to keep, or NULL.}
}
\value{
The subset data.frame, or `meta` unchanged when there is
  nothing to do; NULL passes through.
}
\description{
Companion to \code{\link{qc_keep_cols}}: keeps the rows of `meta`
whose `samples` column matches `selected`. NULL `selected` returns
`meta` unchanged. NULL `meta` passes through. If `meta` has no
`samples` column, returns it unchanged.
}
\examples{
m <- data.frame(samples = c("a", "b", "c"), x = 1:3)
qc_keep_meta_rows(m, c("a", "c"))
}
