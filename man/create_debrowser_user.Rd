% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_signup.R
\name{create_debrowser_user}
\alias{create_debrowser_user}
\title{Create a debrowser user from the R console.}
\usage{
create_debrowser_user(user_id, password, email = NA_character_)
}
\arguments{
\item{user_id}{Username for login.}

\item{password}{Plaintext password (will be hashed via scrypt).}

\item{email}{Optional email. Default \code{NA_character_}.}
}
\value{
The created user_id, invisibly.
}
\description{
Opens \code{users.sqlite}, writes a new \code{kind = "shinymanager"} row,
and closes the connection. Use this to bootstrap the first user when
running \code{startDEBrowser(hosted = TRUE)} for the first time, since
the in-app signup modal lives behind the login wall and is unreachable
until you have an account.
}
\examples{
\donttest{
create_debrowser_user("alice", "hunter2!", "alice@example.com")
startDEBrowser(hosted = TRUE)  # then log in as alice / hunter2!
}
}
