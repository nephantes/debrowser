% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_prep_data.R
\name{merge_comparisons}
\alias{merge_comparisons}
\title{Merge per-comparison DE results into one wide table.}
\usage{
merge_comparisons(dc, nc, params = list())
}
\arguments{
\item{dc}{List of per-comparison containers (each has `init_data`, `cols`,
`cond_names`).}

\item{nc}{Number of comparisons.}

\item{params}{Named list with `norm_method`.}
}
\value{
Merged data.frame (samples + per-comparison foldChange/padj cols).
}
\description{
Pure version of `getMergedComparison()`. The legacy version reads
`input$norm_method`; this takes the same value off `params`.
}
\examples{
init1 <- data.frame(
  S1 = c(100L, 40L), S2 = c(200L, 60L),
  S3 = c(10L, 150L), S4 = c(12L, 160L),
  foldChange = c(10, 0.25), padj = c(0.01, 0.01),
  row.names = c("GeneA", "GeneB")
)
dc <- list(list(
  init_data = init1,
  cols = c("S1", "S2", "S3", "S4"),
  cond_names = c("Treat", "Ctrl")
))
merge_comparisons(dc, nc = 1, params = list(norm_method = "none"))
}
